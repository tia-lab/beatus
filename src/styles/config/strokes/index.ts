import { MEDIA } from '@config'
import SCSSVars from '../types'

export const strokesVars = {
  stroke: {
    icon: { default: '4px', [MEDIA.tablet]: '3px' }
  }
}

export const strokes: SCSSVars = [strokesVars, []]
